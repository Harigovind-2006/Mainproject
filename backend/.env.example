# Server Configuration
PORT=8000
HOST=0.0.0.0
ENVIRONMENT=development

# JWT Authentication (for Dashboard)
SECRET_KEY=fleet-progress-eta-super-secret-key-change-in-production-2025
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Vehicle Authentication Secret
VEHICLE_SECRET_KEY=fleet_device_secret_secure_key_123

# MongoDB Atlas Database (Optional for dev fallback)
MONGODB_URI=
DATABASE_NAME=fleet_monitoring

# Route & Control Default Parameters
DEFAULT_NUM_LANES=4
DEFAULT_LANE_LENGTH=50.0
DEFAULT_LANE_SPACING=5.0
DEFAULT_PLANNED_SPEED=1.5
DEFAULT_TURN_SPEED=0.8
V_MIN=0.5
V_MAX=2.5
FIXED_STEP_SIZE=0.1
LOOKAHEAD_SECONDS=3.0
